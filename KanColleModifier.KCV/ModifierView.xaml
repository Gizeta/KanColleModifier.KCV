<UserControl x:Class="Gizeta.KanColleModifier.KCV.ModifierView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <Button Content="{Binding ModifierButtonContent}" Command="{Binding SwitchModifierOn}"/>
            <Button Content="更新魔改列表" Command="{Binding UpdateModifierList}"/>
            <Button Content="清除魔改缓存" Command="{Binding CleanCache}"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Hidden">
                <TextBlock Text="{Binding ModifierListContent}" Foreground="White"/>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</UserControl>
